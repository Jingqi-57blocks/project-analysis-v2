/**
 * The behavior contract: behavior fact kinds, the relations between them, and the
 * integrity constraints a store and validator enforce. Behavior facts reuse the
 * shared-fact envelope; this is not a parallel model.
 */

export * from "./schema.js";

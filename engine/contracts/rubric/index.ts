/**
 * The acceptance rubric: coverage accounting over the applicability buckets,
 * finding severities, and the gates (with fixed golden-slice thresholds) each
 * milestone is measured by. Objective and recomputable — never "the artifact
 * exists" or "the agent judged it good enough".
 */

export * from "./severity.js";
export * from "./accounting.js";
export * from "./gates.js";

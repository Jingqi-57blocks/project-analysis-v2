# Engine

Deterministic analysis.

- `targets/` — the real projects the engine is developed and graded against.
  Targets are read-only: nothing here writes to them.

The analysis pipeline itself lands from MVP 2 onward (57B-175).
